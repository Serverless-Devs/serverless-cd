steps:
  - run: npm install --registry=https://registry.npmmirror.com
    working-directory: ./master
  - run: npm install --registry=https://registry.npmmirror.com
    working-directory: ./worker
  - run: npm install --registry=https://registry.npmmirror.com
    working-directory: ./admin
  - run: npm install --registry=https://registry.npmmirror.com
    working-directory: ./admin/public
  - run: npm run build
    working-directory: ./admin/public
